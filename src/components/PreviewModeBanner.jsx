export function PreviewModeBanner() {
  return (
    <div className="border-b border-accent/30 bg-accent-tint px-4 py-2 text-center text-xs text-accent sm:px-6">
      This is an employee selection / session tool, not a login — no password is used. Selecting
      your name links this browser session to your employee record for the current session only.
    </div>
  )
}
